const sendMessageController = ({ text, file, tlength, receiver }) => {
  console.log(text);
  console.log(file);
  console.log(tlength);
  console.log(receiver);
};
export default sendMessageController;
